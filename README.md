# ESP32-C6-LCD-1.47-MQTT-BROKER

Visual Studio Code with ESP-IDF  

For configure WiFi use terminal connection via USB (VCP) on 115200 baud rate.
Usage: SSID <SSID> <SSID PWD> <MODE>
    <MODE>: 0-AP(default), 1-Client